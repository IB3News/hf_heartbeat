   on:
     schedule:
       - cron: "0 */12 * * *"
     workflow_dispatch: {}

   jobs:
     ping:
       runs-on: ubuntu-latest
       steps:
         - name: Ping HF Space
           run: curl -sSL --max-time 30 "${{ secrets.HF_SPACE_URL }}" || true
